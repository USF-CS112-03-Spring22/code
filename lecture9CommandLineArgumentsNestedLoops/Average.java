public class Average {
	// Computes the average of the command line arguments provided by the user

	public static void main(String[] arguments) {
		double average = 0;
		int sum = 0;

		if (arguments.length > 0 ) {

			// FILL IN CODE
			// Computer the sum of arguments. Use Integer.parseInt method to convert a string representation of a number to a number


		}
		// FILL IN CODE: compute the average

		System.out.println("The average is " + average);

		
		// Converting a string to an integer
		/*String s = "123";
		int x = Integer.parseInt(s);
		System.out.println(x); */

	}


}
